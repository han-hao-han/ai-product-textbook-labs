# 检查点3 v2运行核验

## 状态

```text
检查点2：通过
检查点3 v2：通过
评价性质：主样本教学性人工裁决
正式验证统计：不纳入
```

## 固定输入

```text
sample_id = zh_train_136
model_run_id = 20260723T082446_019382Z
model = qwen3.7-plus-2026-05-26
prompt = v1
schema = v1
```

检查点3不调用模型，读取检查点2保存的同一次结果。

## 第一层：XFUND原始标注一致性

```text
XFUND原始字段数：26
完全一致：0
规范化后一致：8
值不一致：1
配对不一致：1
模型输出中未找到：16
数据集原始标注中未找到：9
数据集原始标注一致率：30.77%
```

该指标只表示模型输出与XFUND显式question-answer关系是否相同，不判断谁更正确。

## 第二层：教学性最终参考结果

用户结合原图和第一轮两层比较完成：

- 复选组只保留人工确认的选中业务值；
- 两行三列和一行三列表格使用带行号字段保留对象关联；
- 删除被XFUND重复关联的说明文字字段；
- 其余字段保持人工确认结果。

最终结果：

```text
人工校正：18条
结构复核：2条
最终参考字段数：19
参考字段识别正确率：100.00%
```

最终比较文件：

```text
results/comparisons/zh_train_136_20260723T082446_019382Z_20260723T091432_796099Z.json
```

## 评价边界

主样本最终参考结果是在模型运行后形成：

```text
evaluation_context.usage = teaching_post_run_adjudication_only
included_in_formal_validation = false
```

因此100.00%只能用于展示“人工如何裁决模型与原始标注的分歧”，不得作为无偏模型准确率或正式固定验证结果。

后续H4范围已修订：6张固定样本只用于视觉模型调用和工程流程验证，不要求语义化参考结果冻结。若未来另行计算正式内容正确率，才需要在模型调用前冻结相应参考结果。
