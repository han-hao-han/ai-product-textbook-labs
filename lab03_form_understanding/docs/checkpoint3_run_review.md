# 检查点3运行核验

## 当前状态

```text
检查点3程序运行：已完成
比较文件技术核验：通过
用户观察确认：已通过
```

## 比较来源

```text
sample_id = zh_train_103
model_run_id = 20260722T173933_965242Z
comparison_run_id = 20260722T175508_526301Z
model = qwen3.7-plus-2026-05-26
prompt_version = v1
schema_version = v1
comparison_version = v1
reference_source = XFUND v1.0显式question-answer关系
```

比较使用检查点2生成的同一次正式模型结果，没有重新调用模型。

## 汇总结果

| 状态 | 数量 |
|---|---:|
| 人工标注参考字段 | 29 |
| 完全匹配 | 20 |
| 规范化后匹配 | 0 |
| 值错误 | 4 |
| 键值错配 | 0 |
| 遗漏 | 5 |
| 额外字段 | 4 |
| 人工复核 | 0 |

参考字段识别正确率：

```text
(20 + 0) / 29 = 0.6897 = 68.97%
```

该名称不得扩展为模型总体准确率、XFUND官方准确率或真实业务准确率。

## 完整性核验

- 29个人工标注参考字段全部计入一种参考状态；
- 28个模型字段全部被匹配或计为额外字段；
- 人工标注转换警告为0；
- 保存结果与终端汇总一致；
- 未发现API Key、Authorization、Bearer或Cookie；
- 比较器未修改模型输出或人工标注参考结果。

## 可观察的规则边界

本次输出体现了以下保守规则：

- 字段名扩展不会自动按同义词判对；
- 模型将相邻多行文字并入值时，会被严格记为值错误；
- 空字符串仍满足Schema v1的“字符串”类型要求，但可能在比较中成为额外字段；
- 程序不会为了提高指标自动放宽上述规则。

改变空字符串约束将涉及已冻结Schema v1，本检查点不自动修改。

## 用户观察

用户确认：

- 正确字段：`性别`，状态为完全匹配；
- 错误字段：`技术负责人意见`，模型将日期文字一并拼入字段值，因此程序严格分类为值错误。

检查点3已通过。该错误说明多行布局可能使模型把相邻文字合并到同一字段值中。
