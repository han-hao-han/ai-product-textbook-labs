# H4验收结论与工程交接摘要

## 结论

通过。

用户于2026-07-23明确确认：

- 单张识别与6张固定验证现场体验完成；
- 固定验证只用于视觉模型调用和工程流程验证；
- 语义相同但字段命名不同的情况属于严格比较限制，不直接判为模型内容错误。

## 已完成的读者体验检查点

- 检查点1：查看XFUND中文真实数据并人工选择样本；
- 检查点2：完成教材主样本第一次视觉结构化识别；
- 检查点3：查看两层比较并完成主样本教学性人工裁决；
- 检查点4：在Streamlit中完成单张识别与6张固定验证。

## 自动测试结果

```text
89 passed in 19.96s
```

测试只使用Mock和本地文件，不产生真实模型调用。

## 真实运行结果

默认模型：

```text
qwen3.7-plus-2026-05-26
```

主样本保留：

- XFUND原始标注严格一致性比较；
- 非裁决性诊断提示；
- 人工确认或校正后的最终参考结果比较；
- 教学性参考字段识别正确率；
- “模型运行后的教学性人工裁决、不纳入固定验证统计”标记。

## 固定验证结果

```text
运行ID：20260723T100048_257627Z
执行方式：固定顺序串行
样本数：6
成功：6
失败：0
识别字段总数：148
warnings总数：3
总模型调用耗时：94.9504秒
平均耗时：15.8251秒
验证完整：是
参考字段识别正确率：不计算
```

6张样本的raw、parsed、current和comparison共24个关联文件均存在。

## Streamlit体验

- 真实调用只由明确勾选和按钮点击触发；
- 单张识别与固定验证使用同一套核心实现；
- 固定验证保持H2冻结顺序；
- 单张失败时记录失败并继续后续样本；
- 页面展示fields、warnings、耗时、状态和完整性；
- XFUND原始标注一致性只作为可选诊断。

## 示例结果状态

主样本和固定验证的完整真实运行结果只保存在本地。已生成不含字段值的本地示例候选元数据；当前明确暂不提交正式示例结果，待教材编写阶段人工选择、脱敏和确认。

## 已知限制

1. 字段名是开放文本。语义相同但分隔符、行号或层级表达不同的字段，严格比较可能显示为“遗漏/额外字段”。
2. 严格比较衡量字段表示的一致性，不等同于模型内容正确率。
3. XFUND原始标注存在多行拆分、复选符号和结构表达问题，只能作为可选诊断。
4. 主样本最终参考结果形成于模型运行后的教学性人工裁决，不是无偏正式评测。
5. 视觉模型输出可能波动；固定日期模型、Prompt v1、Schema v1和temperature 0只能提高复现性，不能保证逐字一致。
6. 隐私策略硬性排除身份证号码；姓名、电话号码等其他信号只记录并人工复核。

## 尚未完成内容

- 正式教材正文；
- 教材截图的选择和编辑；
- 经人工脱敏并确认可提交仓库的正式示例结果。

## Git状态

本轮未创建Commit、未Push、未Tag、未Release。完整运行记录和原始数据保持本地，不上传仓库。

## 最终状态表述

```text
实验工程与读者体验流程完成，正式教材正文待人工编写
```
