# 附加实验 H4 自动验收说明

## 当前结果

- 验收脚本：`run_acceptance_addon.py`
- 报告文件：`outputs/addon_acceptance_report.json`
- 当前状态：自动验收通过，ok=true

## 覆盖检查

- 必需文件检查；
- 阶段门状态检查；
- v2 结构和 evidence 校验结果检查；
- v2 金标准语义对照检查；
- 三个仓库内 QMSum 转换样例范围检查；
- 完整数据集本地缓存忽略规则检查；
- API 调用预算检查；
- 敏感文本扫描。

## 自动验收结果

- 必需文件：通过；
- 阶段门状态：通过；
- v2 结构和 evidence 校验：通过；
- v2 金标准语义对照：8/8，通过；
- 三样例数据范围和本地缓存忽略规则：通过；
- API 预算：37/50，通过；
- 敏感文本扫描：无命中。

## 人工验收重点

- 是否认可 QMSum TS3010a 作为附加实验主样例；
- 是否认可 v2 Prompt 调整后的 8/8 设计要求召回结果；
- 是否认可“证据需确定性修复”作为真实会议转写的已知限制；
- 是否认可仓库只保留三个样例，完整 QMSum 由用户本地下载且不提交仓库；
- 是否认可该实验只说明小规模适配边界，不代表完整 QMSum benchmark。
