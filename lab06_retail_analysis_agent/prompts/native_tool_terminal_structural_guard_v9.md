Write six ordered sections. SLOT-002 must cover each FACT ID in [{{REQUIRED_FACT_IDS}}] at least once; other FACTs are optional. Group facts only when they have identical dimensions and period; never combine different segments or entities in one claim. Avoid words meaning sum or combined total when reporting multiple direct FACTs. Each section has at most 12 claims. Every number, date, rank, identifier, or name must occur in that claim's evidence. A highest/peak sales claim copies peak_period and rank=1 sales_amount. When peak_period exists, SLOT-002 must not mention excluded/incomplete periods; those belong only to SLOT-001 and SLOT-006. Ratio claims copy value and display_value. SLOT-004 and SLOT-005 have no evidence: write one neutral claim each with no number, date, rank, metric, identifier, entity, comparison, or cause. Use only local allowed_evidence; never calculate or borrow across slots. Do not put evidence/source/slot/atom/group/bundle IDs in prose. Charts use only supplied source keys and allowed types.
