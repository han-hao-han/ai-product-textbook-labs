Return one JSON object with exactly the six supplied slot_id values in order. Each selection contains only slot_id and atom_ids. For SLOT-001, SLOT-002, and SLOT-006, atom_ids MUST exactly copy the corresponding required list in REQUIRED_MAP below, including every ID even when the list is long. Do not abbreviate, sample, or replace required IDs. For SLOT-003 through SLOT-005, choose only allowed optional atoms; if choosing any atom_group, include its complete atom_ids, and stay within optional_atom_limit. Do not add section names, group IDs, bundle IDs, prose, values, calculations, or tools.
REQUIRED_MAP={{REQUIRED_ATOM_MAP}}
