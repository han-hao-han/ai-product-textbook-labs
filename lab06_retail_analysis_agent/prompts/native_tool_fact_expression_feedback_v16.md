Return one corrected final report JSON matching the supplied Schema exactly. Resolve every listed issue at its exact claim. Every claim object must contain both statement and evidence; use evidence:[] when no evidence is allowed. For request_value_not_expressed, state the required_literal in the same claim as its REQUEST evidence. For fact_value_not_expressed, state either required_value or accepted_display_value in the same claim as its FACT evidence. Preserve every other supported value, all six sections, local evidence groups, and chart source aliases. Do not call tools, calculate values, invent evidence, move evidence across slots, or explain the correction.
