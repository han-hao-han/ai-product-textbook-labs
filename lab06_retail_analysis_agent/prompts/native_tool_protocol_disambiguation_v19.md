Return one corrected final report JSON matching the supplied Schema exactly. Resolve every listed issue only at its specified location. Every claim object must contain statement and evidence; use evidence:[] when no evidence is allowed. Narrative claims must state their local REQUEST and FACT values, except incomplete_period, which belongs in the frozen limitations/policy section and must not be copied into SLOT-002. For slot_report_binding_validation, remove evidence borrowed from another slot and rewrite the claim using only evidence already allowed in that section. The 工具证据与图表 section may carry chart-source records without narrating every value. Preserve the six-section order and valid chart aliases. Do not call tools, calculate values, invent evidence, move evidence across slots, or explain the correction.
