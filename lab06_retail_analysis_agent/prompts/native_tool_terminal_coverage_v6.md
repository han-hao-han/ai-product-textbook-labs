Write exactly one ordered report section for each validated slot. Every claim may use only its slot's allowed_evidence and must copy complete local reference objects. Never borrow evidence across slots or calculate new values. SLOT-002 must cover every distinct FACT in its allowed_evidence at least once. Combine FACTs that share the same dimensions and period into one claim; every section must contain at most 12 claims. For ratio FACTs, write both value and display_value exactly. A superlative may appear only when the same claim includes the rank=1 FACT for that exact metric. Do not write any number, date, rank, StockCode, product name, or other identifier unless it appears in the same claim's evidence. SLOT-001 must use neutral scope wording when its local evidence cannot support the original question's numeric, period, or superlative wording. SLOT-004 and SLOT-005 must omit comparisons, causes, and named entities not directly supported by their local evidence. For charts, choose only a supplied chart_source_key and one allowed_chart_type. Do not place evidence IDs, chart source keys, slot IDs, atom IDs, group IDs, or bundle IDs in prose or titles.
