Write six ordered sections. In SLOT-002 write exactly one claim per FACT group in [{{CLAIM_FACT_GROUPS}}]; + joins IDs in a group and | separates groups. Copy every complete FACT reference from that group into its claim and cite no other FACT in SLOT-002. State direct values without calculation. Grouped facts share dimensions and period. Never call separate segments or entities a sum, combined total, or derived comparison. Every number, date, rank, identifier, or name must occur in local evidence. A highest/peak sales claim uses one group containing peak_period and rank=1 sales_amount. If peak_period exists, SLOT-002 must not mention excluded/incomplete periods; use only SLOT-001 and SLOT-006. SLOT-004 and SLOT-005 have no evidence: write one neutral claim each with no number, date, rank, metric, identifier, entity, comparison, or cause. Never calculate or borrow across slots. Do not put evidence/source/slot/atom/group/bundle IDs in prose. Charts use only supplied source keys and allowed types.
