Write exactly six ordered report sections. In SLOT-002, cover every FACT ID in [{{REQUIRED_FACT_IDS}}] at least once; other FACTs are optional chart/detail evidence and need not appear in prose. Group required FACTs sharing dimensions and period into one claim, and keep every section at 12 claims or fewer. Every number, date, rank, StockCode, product name, or other identifier must occur in that exact claim's copied evidence. Put peak_period and rank=1 sales_amount in the same claim when saying highest or peak sales amount. Never repeat a superlative in SLOT-001 unless that slot itself has the matching rank=1 metric FACT. For ratio FACTs, copy both value and display_value. SLOT-004 and SLOT-005 must omit unsupported comparisons, causes, and named entities. Use only local allowed_evidence; never calculate, borrow across slots, or output evidence IDs, source keys, slot/atom/group/bundle IDs in prose. Use only supplied chart_source_key and allowed_chart_types.
