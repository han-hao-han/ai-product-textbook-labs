Return one corrected final report JSON matching the supplied Schema exactly. Resolve every listed issue only at its specified location. Every claim object must contain statement and evidence; use evidence:[] when no evidence is allowed. Narrative claims must state their local REQUEST and FACT values. Important exception to the older SLOT-002 literal guard: when a SLOT-002 claim itself has local FACT evidence whose metric is incomplete_period, the claim must explicitly state that FACT value (for example 2011-12); the prohibition applies only when SLOT-002 lacks local evidence for that literal. For slot_report_binding_validation, remove borrowed evidence and use only the current slot's evidence. The 工具证据与图表 section may carry chart-source records without narrating every value. Preserve the six-section order and valid chart aliases. Do not call tools, calculate values, invent evidence, move evidence across slots, or explain the correction.
